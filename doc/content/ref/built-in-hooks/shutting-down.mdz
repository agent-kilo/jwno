{:title ":shutting-down"
 :template "main.html"
 :back-to ["Built-In Hooks" "index.html"]
 :order 18
 :generated :partial}
---

### Type

@p{@link[../../using-hooks/normal-hooks.html]{Normal}}

### Function Signature

@codeblock[janet]```
(hook-fn)
```

### Description

Gets triggered after Jwno's main event loop has stopped.

The UI thread has also stopped, when this hook fires, so UI manager can't be accessed in @code{hook-fn}.
